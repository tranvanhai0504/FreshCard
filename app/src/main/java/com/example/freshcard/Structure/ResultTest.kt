package com.example.freshcard.Structure

import com.google.type.DateTime

data class ResultTest(val idUser: String, val amountCorrect: Int, val duration: Long, val time: DateTime, val type: String){}
